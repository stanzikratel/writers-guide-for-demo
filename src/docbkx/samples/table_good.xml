<?xml version="1.0" encoding="UTF-8"?>
<table border="1" frame="box">
    <caption>Request Methods</caption>
    <thead>
        <tr>
            <td>Request Method</td>
            <td>Description</td>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>Complex MIME Type with Vendor Specification</td>
            <td>
                <para> In the MIME type for the <code>Accept</code>
                    HTTP header, a request may specify which
                        <emphasis>vendor namespace</emphasis> it is
                    expecting to format the response. </para>
                <para> For example, in
                        <code>application/vnd.rackspace-v1.0+xml</code>,
                    the vendor namespace has been specified as
                        <code>vnd.rackspace</code> and version
                        <code>v1.0</code> of the service API has been
                    requested. </para>
                <para> Vendor specifications do not modify how the
                    versioning service routes requests. The
                    information is still parsed and stored, however,
                    and may be utilized by other components further
                    down the stack. </para>
            </td>
        </tr>
        <tr>
            <td>Complex MIME Type with Version Specification</td>
            <td>
                <para> In the MIME type for the <code>Accept</code>
                    HTTP header, a request may specify which
                        <emphasis>version</emphasis> it is expecting
                    to process the response. </para>
                <para> For example, in
                        <code>application/v1.0+xml</code>, version
                        <code>v1.0</code> of the service API has been
                    requested. </para>
            </td>
        </tr>
    </tbody>
</table>
